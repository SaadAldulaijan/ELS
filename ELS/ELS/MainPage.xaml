<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:ELS.ViewModels"
             x:Class="ELS.MainPage">

    <ContentPage.BindingContext>
        <local:MainPageViewModel />
    </ContentPage.BindingContext>

    <StackLayout>
        <Image Source="ELS.png"/> 

        <StackLayout>
            <Picker Title="Select location"
                    ItemsSource="{Binding Locations}"
                    ItemDisplayBinding="{Binding Name}"
                    SelectedItem="{Binding SelectedLocation}"
                    Margin="5">
            </Picker>

            <Picker x:Name="subDistinct"
                    Title="Select Sub distinct"
                    ItemsSource="{Binding SubLocation}"
                    ItemDisplayBinding="{Binding SubLocation.Name}"
                    SelectedItem="{Binding SelectedSubLocation}"
                    Margin="5">
            </Picker>
        </StackLayout>

        <Button Command="{Binding OpenGoogleMap}"
                Text="OPEN GOOGLE MAP"
                TextColor="White"
                BackgroundColor="#009FDF"
                Margin="5">
        </Button>
    </StackLayout>
</ContentPage>
